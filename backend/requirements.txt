# Phase 0: just enough to boot the API and talk to Postgres via async
# SQLAlchemy + Alembic migrations. Remaining ML/NLP libraries (PyTorch,
# spaCy, Sentence-Transformers, XGBoost/LightGBM) are added in the phases
# that actually use them (see docs/ROADMAP.md) rather than installed
# speculatively here.
#
# Lower-bound constraints, not exact pins — run
#   pip install -r requirements.txt && pip freeze > requirements.lock.txt
# after the first install and commit the lockfile for reproducible builds.

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
SQLAlchemy>=2.0.35
asyncpg>=0.29
alembic>=1.13
python-dotenv>=1.0

# Phase 1: document parsing
PyMuPDF>=1.24
python-docx>=1.1

# Phase 3: NLP information extraction. The en_core_web_sm language model
# isn't a normal PyPI package — install it with:
#   python -m spacy download en_core_web_sm
spacy>=3.7

# Phase 5: skill taxonomy (pgvector SQLAlchemy type for skills.embedding)
pgvector>=0.3

# Phase 6: embedding service
sentence-transformers>=2.7

# Phase 8: API wiring (multipart form/file upload parsing)
python-multipart>=0.0.9
