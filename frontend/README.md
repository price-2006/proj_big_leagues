# Frontend

Not yet scaffolded — the React + TypeScript app is built in Phase 9, once there's a working backend API (Phase 8) for it to consume. See `docs/ARCHITECTURE.md` §11 for the planned component structure and `docs/ROADMAP.md` Phase 9 for what "testable" means for that phase.
